{
    "title": "EPREL Scraper Input",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "startUrls": {
            "title": "Start URLs",
            "type": "array",
            "description": "EPREL product group URLs or specific product detail URLs.",
            "editor": "requestListSources",
            "prefill": [
                { "url": "https://eprel.ec.europa.eu/screen/product/refrigeratingappliances2019" }
            ]
        },
        "maxResults": {
            "title": "Maximum Results",
            "type": "integer",
            "description": "Limit the number of products to scrape.",
            "default": 50,
            "minimum": 1
        },
        "proxyConfiguration": {
            "title": "Proxy Configuration",
            "type": "object",
            "description": "EPREL might rate limit; using proxies is recommended.",
            "editor": "proxy",
            "default": { "useApifyProxy": true }
        }
    },
    "required": ["startUrls"]
}